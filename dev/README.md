Files in this folder are developer resources like for example git hooks or IDE configuration files.

The file example.git commit contains some aliases that can enhance git productivity, to use those aliases, go to the home of your local git installation and find the .gitconfig file. Add the content of the example.gitconfig file to the end of it 
On windows it's situated C:/user/username, on linux/mac, /home/username